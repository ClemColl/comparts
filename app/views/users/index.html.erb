<% content_for :meta_title, "Homepage" %>
<% content_for :meta_description, "Description" %>
<%# content_for :meta_image, cloudinary_url(@offer.photo.path) %>
<div class="formNew">
<h1>Artistes à découvrir</h1>
<div id="separator"></div>
<% @artists.each do |a| %>
	<% if a[:role_id] == 'Artiste' %>
	<div style="width:30%;height:100px;display:inline-block;">
 		<%= a.nom %><br>
 		<%= link_to 'Voir son profil', user_path(a.id) %>
		<hr>
	</div>
	<% end %>
<% end %>
</div>